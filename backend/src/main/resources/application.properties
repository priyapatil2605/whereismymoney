spring.application.name=whereismymoney-backend

# ===============================
# Database
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5433/whereismymoney
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}

# ===============================
# JPA
# ===============================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Flyway
# ===============================
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=false

# ===============================
# Server
# ===============================
server.port=8080

# ===============================
# Actuator
# ===============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Claude API

claude.api.key=${CLAUDE_API_KEY:}
claude.api.url=https://api.anthropic.com/v1/messages
claude.model=claude-sonnet-4-6

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
logging.level.org.springframework.security=TRACE


spring.application.name=whereismymoney-backend

spring.datasource.url=jdbc:postgresql://localhost:5433/whereismymoney
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=false

server.port=8080

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

claude.api.key=${CLAUDE_API_KEY}
claude.api.url=https://api.anthropic.com/v1/messages
claude.model=claude-sonnet-4-6

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

ai.insights.enabled=${AI_INSIGHTS_ENABLED:false}

admin.email=${ADMIN_EMAIL:admin@whereismymoney.local}